table.getTableHeader().setReorderingAllowed(false);
// em todas as tabelas

linha 256 tela manipular cliente
else {
    JOptionPane.showMessageDialog (contentPane, "Saldo negativo, não é possível remunerar");
}
